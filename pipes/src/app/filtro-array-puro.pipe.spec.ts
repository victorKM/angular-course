import { FiltroArrayPuroPipe } from './filtro-array-puro.pipe';

describe('FiltroArrayPuroPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroArrayPuroPipe();
    expect(pipe).toBeTruthy();
  });
});
